{
  "name": "cron-parser-lite",
  "version": "1.0.0",
  "description": "Parser minimalista de expressoes cron com calculo de proxima execucao",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["cron", "parser", "schedule", "next", "crontab"],
  "author": "Tox",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
